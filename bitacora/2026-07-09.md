# Sesión 2026-07-09

## Cambios
- **Fase 1 completa**: scaffold Astro v7 + adaptador Cloudflare Workers + Tailwind CSS v4
- Paleta de 5 tokens (`bg`, `surface`, `accent-soft`, `primary`, `ink`) y tipografía (Fraunces + Nunito Sans) vía `@fontsource`
- 6 páginas estáticas con placeholders: `/`, `/inicio`, `/quien-soy`, `/sesiones`, `/tameana`, `/gracias`
- Animación de respiración del logo en la intro (`@keyframes breathe`, 4.5s, respeta `prefers-reduced-motion`)
- Header persistente con nav en páginas internas
- Componentes aislados: `Logo`, `Icon`, `ServiceCard`, `Header`, `CTALink`, `BreathingLogo`
- View Transitions nativas del navegador (meta tag `view-transition`)
- SEO: sitemap (`@astrojs/sitemap`), OG tags, `robots.txt`, `favicon.svg`
- Wrangler types generados (`worker-configuration.d.ts`), `.dev.vars.example`
- Deploy inicial a Cloudflare Workers: **https://tameana.marin1882.workers.dev**

## Commits
- (no se ha iniciado repo git aún)

## Pendiente
- **Fase 2 — Reservas**: D1 + esquema Drizzle + migraciones + seed, generación de huecos, `/reservar` con Turnstile y emails vía Resend
- **Fase 3 — Admin**: login con contraseña, listado de reservas, confirmar/cancelar con emails
- Inicializar repo git y hacer commit inicial
- Sustituir placeholders por textos reales (marcados con `<!-- TODO -->`)
- Ajustar `site` en `astro.config.ts` cuando haya dominio propio (ahora usa `tameana.com` como placeholder)
