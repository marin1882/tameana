## Cambios

- **Iconos dibujados (14 SVGs)**: Creado `DrawnIcon.astro` que importa SVGs de `src/icons/drawn/`, reemplaza `fill="black"` → `fill="currentColor"` y renderiza inline. Aplicados en `/inicio` (quien-soy, sesiones, tameana) y `/tameana` (adultos, ninos, espacios, animales, grupal). 6 iconos pendientes de ubicación: cartas, contacto, online, preguntas-frecuentes, presencial, testimonios.
- **Modalidad (presencial/online)**: Nueva columna en `bookings`, migración D1 (0002), página `/reservar/modalidad`, redirección si falta, campo oculto en formulario, visible en emails y admin.
- **Ubicación (Barcelona / Otras localidades)**: Nueva página `/reservar/ubicacion` para presencial. `/reservar/contacto` con formulario + acción `sendLocationInquiry` (Turnstile + email a terapeuta). Online va directo al calendario.
- **Git init + GitHub**: Repo `marin1882/tameana` creado y conectado a Cloudflare Workers Builds.
- **Auditoría de seguridad**: Detectado hardcoded password fallback en middleware (`tameana-admin-2026`), endpoint test-email público, info leakage en errores API, 14 vulns npm (undici). Pendiente de corregir.

## Commits

- `899c579` Añadir paso de ubicación para reservas presenciales
- `3abdfbc` Añadir modalidad (presencial/online) al flujo de reserva
- `bf4543b` Test: verificar conexión Cloudflare Workers Builds
- `9ff66b0` Primer commit: sitio Tameana con Astro + Cloudflare Workers

## Pendiente

- **🔴 Crítico**: Corregir hardcoded password fallback en `src/middleware.ts:25`
- Corregir endpoint `/api/test-email` público (añadir auth o eliminar)
- Sanear info leakage en errores de API routes
- `npm audit fix` para vulnerabilidades de undici
- Decidir ubicación de los 6 iconos drawn no usados aún (cartas, contacto, online, preguntas-frecuentes, presencial, testimonios)
- Añadir CSP y security headers
