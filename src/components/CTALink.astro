---
export interface Props {
  href: string;
  variant?: 'primary' | 'ghost';
}

const { href, variant = 'primary' } = Astro.props;
---

<a href={href} class={variant === 'primary' ? 'btn-primary' : 'btn-ghost'}>
  <slot />
</a>
